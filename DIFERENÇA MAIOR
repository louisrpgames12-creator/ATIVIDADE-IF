#include <stdio.h>

int main() {
    int num1, num2, maior;
    
    printf("ME INFORME DOIS NUMEROS INTEIROS: ");
    scanf("%d%d", &num1, &num2);
    
    if(num1 > num2) {
        maior = num1 - num2;
        printf("a diferenca e: %d", maior);
    } else
        if(num1 < num2) {
            maior = num2 - num1;
            printf("a diferenca e: %d", maior);
        } else
            printf("nao tem maior");
            


    return 0;
}
